local Class = require 'src.class'

local SceneLayer = Class('SceneLayer')

-- Default constructor. No real behaviour
function SceneLayer.new(inst)
end

function SceneLayer:update(dt)
end

function SceneLayer:draw(dt)
end